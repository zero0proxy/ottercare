---
// src/components/Hero.astro
import LottieDog from './LottieDog.astro';
---
<section id="hero" class="hero-section">
    <div class="hero-content">
        <h1>На страже всех наших братьев меньших!</h1>
        
        <LottieDog />
        
        <p>Каждая мелочь для вас — возможно, счастье для кого-то другого</p>
        <a href="/donate/" class="cta-button">Помочь мохнатым</a>
    </div>
</section>

<style>
    .hero-section {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        height: 80vh;
        min-height: 500px; /* Немного увеличим мин. высоту */
        color: var(--color-text-dark);
        background-color: transparent;
        position: relative;
        overflow: hidden;
    }

    .hero-content {
      z-index: 10;
      position: relative;
      
      /* 2. Гарантируем, что все элементы (h1, Lottie, p, a) 
         будут идти друг под другом и по центру */
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .hero-content h1 {
        font-size: 3.5rem;
        margin-bottom: 0; /* Убираем отступ у h1 */
    }

    .hero-content p {
        font-size: 1.3rem;
        margin-top: 0; /* Убираем отступ у <p> */
        margin-bottom: 2rem;
    }

    .cta-button {
        background: var(--color-primary-purple);
        color: white;
        padding: 1rem 2rem;
        text-decoration: none;
        font-size: 1.2rem;
        font-weight: bold;
        border-radius: 5px;
        transition: background-color 0.3s;
        z-index: 10;
        position: relative;
    }
    .cta-button:hover {
        background: var(--color-primary-purple-hover);
    }

    @media (max-width: 768px) {
        .hero-content h1 {
            font-size: 2.5rem;
        }
        .hero-content p {
            font-size: 1rem;
        }
    }
</style>